global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "url_shortener_nodes"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api1:8000", "api2:8000"]
        labels:
          cluster: "url-shortener-prod"
