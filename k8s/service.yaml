apiVersion: v1
kind: Service
metadata:
  name: url-shortener-service
  labels:
    app: url-shortener
spec:
  type: ClusterIP
  selector:
    app: url-shortener
    tier: api
  ports:
    - name: http
      port: 80
      targetPort: 8000
